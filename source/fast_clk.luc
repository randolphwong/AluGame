module fast_clk (
    input clk,  // clock
    input rst,  // reset
    output out // slowed down clock
  ) {
  
  edge_detector rising_edge(#RISE(1), #FALL(0), .clk(clk));
  dff counter[22](.clk(clk), .rst(rst));
  
  always {
    rising_edge.in = counter.q[21];
    counter.d = counter.q + 1;
    out = rising_edge.out;
  }
}
